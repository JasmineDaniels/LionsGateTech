// module.exports = {
//     formatDate: (date) => {
//         return date.toLocalString('en-US')
//     }
// } 

// Handlebars.registerHelper('format_date', function (createdAt) {
//     return createdAt.toUpperCase()
// })
